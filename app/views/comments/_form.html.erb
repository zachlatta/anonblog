<%= form_for([@post, @post.comments.build]) do |f| %>
  <fieldset>
    <%= f.text_area :body, :rows => '8' %>
    <div class="actions">
      <%= f.submit :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
